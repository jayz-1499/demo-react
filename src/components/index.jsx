import React from 'react';
import PropTypes from 'prop-types';

index.propTypes = {
    
};

function index(props) {
    return (
        <div>
            <h1>Hải Đẹp trai</h1>
        </div>
    );
}

export default index;